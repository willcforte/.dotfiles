# neovim, tmux, btop, gh, tree, lazygit, lazydocker are managed by
# home-manager — see stow/nix/.config/home-manager/home.nix

# Git (bootstrap — needed before nix/home-manager exist)
git
git-lfs

# Build tools
build-essential

# Dev / shell utilities
stow
flameshot
flatpak
cron
tldr
lsd
unzip

# Python
python3-full

# SSH / networking
openssh-server

# Third-party repos (added by install.sh before this list is installed)
tailscale
