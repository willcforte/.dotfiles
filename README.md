# Dotfiles

This repository is intended to be cloned at `~/.dotfiles`.

There are various stow directories that can be referred to by name, e.g. `claude`.

To use, simply enter the `stow` directory and run `stow -t ~ <dirname>`.

This will create symlinks that port the folder's contents to `~`.

